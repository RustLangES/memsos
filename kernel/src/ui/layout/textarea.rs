use super::{Layout, LayoutChild, LayoutArgs, vertical::VerticalLayout};

pub struct TextArea {
    layout: VerticalLayout, 
}

impl TextArea {
    pub const fn new() -> Self {
        todo!();
    }
}
